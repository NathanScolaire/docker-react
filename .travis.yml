language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t ethnatscolaire/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true ethnatascolaire/docker-react npm run test -- --coverage
